#include <iostream>

// just a matrix class (numpy array in cpp)
class Tensor {
    public:
        Tensor(int rows, int cols) {
            // new
        }

        ~Tensor(void) {
            // delete
        }
    private:
};
